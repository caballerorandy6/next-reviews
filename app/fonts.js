import { Orbitron } from "next/font/google";

export const orbitron = Orbitron({
  subsets: ["latin"],
  variable: "--font-orbitron",
});

export const exo2 = Orbitron({
  subsets: ["latin"],
  variable: "--font-orbitron",
});
